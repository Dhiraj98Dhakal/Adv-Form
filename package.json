{
  "name": "advance-computer-center",
  "version": "1.0.0",
  "description": "Student Registration System",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": "18.x"
  }
}